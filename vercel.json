{
  "version": 2,
  "buildCommand": "npm install && npm --prefix client install && npm --prefix client run build && mkdir -p public && touch public/index.html",
  "installCommand": "npm install",
  "outputDirectory": "public",
  "functions": {
    "api/index.js": {
      "memory": 1024,
      "maxDuration": 60
    }
  }
}
